services:
  # cath-af-cli:
  #   build: docker/cath-af-cli

  script:
    build: docker/script

  pdb-tools:
    build: docker/pdb-tools

  unidoc:
    platform: linux/amd64
    build: docker/unidoc

  chainsaw:
    build: docker/chainsaw

  merizo:
    build: docker/merizo
